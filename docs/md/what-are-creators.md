In order to be evaluated, **creators must export a default function**.
This function **will be given as first argument a `buildPage` function**, that you creator can call anytime in order to build a page.
